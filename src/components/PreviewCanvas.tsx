// src/components/PreviewCanvas.tsx（占位，Task 11 实现）
type Props = {
  preview: { dataUrl: string; width: number; height: number } | null;
  solo: boolean;
  soloName?: string;
  onExitSolo: () => void;
};

export default function PreviewCanvas(props: Props) {
  void props;
  return null;
}
