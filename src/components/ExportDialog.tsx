// src/components/ExportDialog.tsx（占位，Task 12 实现）
import type { AppState, Snapshot } from "../types";

type Props = { state: AppState; snapshot: Snapshot; onClose: () => void };

export default function ExportDialog(props: Props) {
  void props;
  return null;
}
